'use client';

import React from 'react';
import ProductDetail from '../../components/ProductDetail'; // Adjust the path if your component is elsewhere

const ProductPage = () => {
  return (
    <div>
      <ProductDetail />
    </div>
  );
};

export default ProductPage;
